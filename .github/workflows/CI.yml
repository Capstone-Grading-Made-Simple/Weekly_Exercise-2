name: Weekly Exercise 2 Test

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build & Test Project
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Codes
        uses: actions/checkout@v2
        with: 
          path: main
        
      - name: Checkout Test Scripts
        uses: actions/checkout@v2
        with:
          repository: Linus-Tay/JUnit_MAD_Exercise-2
          path: tests
          
      - name: Check files
        run: |
            cd main
            ls
            cd app
            ls
            cd src 
            ls
            cd main
            ls
            
      - name: Create Test Files
        run: |
          cp tests/Exercise_Test-2.kt main/app/src/test/java/com/example/week2exercise/UserTest.kt
          cp tests/MainActivity_Test.kt main/app/src/androidTest/java/com/example/week2exercise/MainActivityTest.kt

      - name: Set Up JDK 11
        uses: actions/setup-java@v1
        with: 
          java-version: 11

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run Tests
        run: ./gradlew test
        
      - name: Android Test Report
        uses: asadmansr/android-test-report-action@v1.2.0
        if: ${{ always() }}

      - name: Publish Unit Test Results
        uses: EnricoMi/publish-unit-test-result-action@v1
        if: always()
        with:
          files: test-results/**/*.xml

      - name: Build Project
        run: ./gradlew assemble
